## 2024-06-12 - [Disable Debug Mode in Production Dockerfile]
**Vulnerability:** The Dockerfile exposed the Fava/Flask debug mode (`ENV FAVA_DEBUG "true"`) in production, which could leak stack traces and potentially internal environment configuration details to end users.
**Learning:** Containerized applications, especially those built to be shared or run in production like fava-docker, should never default to development debug modes due to the inherent risk of information disclosure via verbose error pages.
**Prevention:** Always default web framework debug settings to false or off in the final stages of Dockerfile builds.

## 2024-06-18 - [Supply Chain Security: Pinning Git Dependencies and Avoiding ADD]
**Vulnerability:** The project relied on mutable git branch references (`@main`, `@master`) and direct branch zip downloads for several dependencies, exposing the build to potential upstream branch poisoning or unexpected breakages. Additionally, the Dockerfile used the `ADD` instruction to copy `requirements.txt`, which can unexpectedly fetch remote URLs or extract archives, introducing a risk of manipulation.
**Learning:** In a containerized build environment, all external dependencies and source files must be explicitly pinned (using exact commit SHAs or version numbers) to guarantee reproducible and secure builds. Using `ADD` when only copying local files violates the principle of least privilege.
**Prevention:** Always use `COPY` for local files in Dockerfiles and avoid mutable references (like branch names) in requirements files; always use exact commit hashes or immutable release tags for git-based dependencies.

## 2024-06-25 - [Fix Python path hijacking vulnerability via PYTHONPATH]
**Vulnerability:** The Dockerfile used `ENV PYTHONPATH "/myData/myTools:$PYTHONPATH"`. Because standard Python base images often do not define `PYTHONPATH`, `$PYTHONPATH` evaluates to an empty string. This results in the environment variable having a trailing colon (`ENV PYTHONPATH="/myData/myTools:"`), which appends an empty string to Python's `sys.path`. In Python, an empty string in `sys.path` means the current working directory. This creates a critical path hijacking/relative path traversal vulnerability, as running the container from a directory containing a malicious Python file (e.g., `os.py`) could cause Python to load the malicious module instead of standard libraries, leading to arbitrary code execution.
**Learning:** Appending undefined environment variables to Python paths in Dockerfiles can unintentionally add the current working directory to `sys.path`. This is a subtle but critical vector for arbitrary code execution in containerized applications.
**Prevention:** Always directly set `PYTHONPATH` (`ENV PYTHONPATH="/myData/myTools"`) if it is not expected to build upon a previously defined value, or safely handle potentially empty variables (though simple assignment is safer and clearer for base definitions).

## 2024-06-26 - [Fix Poisoned Pipeline Execution in GitHub Actions]
**Vulnerability:** The GitHub Actions workflow `push-to-docker-hub.yaml` logged into Docker Hub and unconditionally pushed images on `pull_request` events using `push: true`. This could allow unauthorized code from malicious pull requests to be built and pushed to the production container registry, a vulnerability known as Poisoned Pipeline Execution (PPE).
**Learning:** CI/CD workflows triggered by pull requests from potentially untrusted sources should never have access to deployment credentials or unconditionally publish artifacts.
**Prevention:** Always use conditional checks (e.g., `if: github.event_name != 'pull_request'`) on steps requiring registry secrets, and dynamically set push flags (e.g., `push: ${{ github.event_name != 'pull_request' }}`) to ensure artifacts are only published from trusted events like merges to the default branch.
